# === App ===
FRONTEND_URL=http://localhost:5173
DEV=true

# === Weather providers (priority: XWeather > Meteoblue > Tomorrow.io > Open-Meteo > WeatherAPI)
# Open-Meteo is free — no key needed. Remaining keys are optional fallbacks.
XWEATHER_CLIENT_ID=
XWEATHER_CLIENT_SECRET=
METEOBLUE_KEY=
TOMORROW_KEY=
WEATHERAPI_KEY=

# === Data source (public — no key required) ===
DATA_GOV_MY_BASE_URL=https://api.data.gov.my
